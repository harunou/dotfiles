#!/bin/sh

command="$1"
shift

case "$command" in

  setup)
    exec "$HOME/.local/bin/harunou-setup" "$@"
    ;;

  config)
    exec "$HOME/.local/bin/harunou-config" "$@"
    ;;

  '') echo "Usage: `basename "$0"` <command> [options]" 
    ;;

  *) 
    echo "`basename "$0"` $command: unknown command." >&2; exit 1 
    ;;
esac
